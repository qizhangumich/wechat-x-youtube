fastapi==0.68.0
uvicorn==0.15.0
requests==2.25.1
pydantic==1.8.2
python-dotenv==0.19.0
pycryptodome==3.19.0
# Phase 3 — media downloader. Unpinned because yt-dlp needs frequent updates
# to keep up with platform changes; rebuild grabs latest at image build time.
yt-dlp
